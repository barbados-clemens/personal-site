{
  "name": "portfoilo",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Caleb Ukle",
  "repository": "https://gitlab.com/caleb-ukle/portfolio",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "lint:fix": "tslint -c tslint.json -p tsconfig.json --fix",
    "deploy": "firebase deploy -m \"$CI_COMMIT_SHORT_SHA - $CI_COMMIT_TITLE\" --token $FIREBASE_TOKEN"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
